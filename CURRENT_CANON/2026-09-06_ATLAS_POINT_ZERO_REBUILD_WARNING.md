# ATLAS Ω — REBUILD WARNING

Do not append the current 27-position list to the Master Universe Prompt as “context” before selection unless it is explicitly masked from selection and used only after the clean result.

Language models can anchor on supplied incumbents even when instructed not to. The strongest operational control is therefore **information separation**: run clean selection first, reveal current operational state second.

This is an anti-anchoring implementation safeguard, not merely a prose instruction.
