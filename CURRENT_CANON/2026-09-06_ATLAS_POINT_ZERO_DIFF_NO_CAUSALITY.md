# ATLAS Ω — PORTFOLIO DIFF ≠ CAUSAL HISTORY

A difference between two portfolio sets shows state change, not the causal sequence that produced it.

Do not infer “A replaced B” unless an explicit selection/execution decision documents that pairwise transition.

This prevents false genealogies when Point Zero rebuilds change many names simultaneously.
