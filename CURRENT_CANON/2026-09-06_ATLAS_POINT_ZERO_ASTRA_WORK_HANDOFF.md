# ATLAS Ω — ASTRA WORK HANDOFF REQUIREMENTS

When Astra/Work executes the next full rebuild, it must receive the Master Universe Prompt as the sole selection instruction and must not be given the current operational 27 as a starting portfolio.

The current 27 may be supplied only after clean selection, for a separate transition-diff phase.

Astra must distinguish:

`CLEAN_REBUILD_RESULT`
from
`CURRENT_OPERATIONAL_STATE`
from
`EXECUTION_TRANSITION_PLAN`.

If the full 487-entity evidence set cannot be refreshed with adequate depth, Astra must label the result partial/shadow rather than pretending to have completed the canonical rebuild.
