# ATLAS Ω — NO INFERRED PORTFOLIO GENEALOGY

Do not reconstruct undocumented chains of replacements from two different portfolio snapshots.

A clean Point Zero rebuild creates a new selected state; it does not imply that every difference versus an older portfolio was a sequential replacement decision.

For the current operational 27, the only explicitly confirmed local change is `GLW OUT → VRT IN` within that snapshot lineage.

Future clean rebuild differences must be labeled as `REBUILD_DIFF` unless an actual execution sequence is separately documented.
