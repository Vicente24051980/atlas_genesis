# ATLAS Ω — POST-REBUILD TRANSITION DIFF SCHEMA

Only after a valid clean rebuild, compare it with the current operational portfolio and report:

`KEEP`
`CLEAN_OUT_EXECUTION_IN`
`CLEAN_IN_EXECUTION_OUT`
`RANK_CHANGE`
`OPTIMAL_N_CHANGE`
`EXECUTION_BLOCKER`
`TAX_TURNOVER_NOTE`
`ENTRY_TIMING_NOTE`

No transition blocker may retroactively alter the clean ranking. It changes implementation state only.
