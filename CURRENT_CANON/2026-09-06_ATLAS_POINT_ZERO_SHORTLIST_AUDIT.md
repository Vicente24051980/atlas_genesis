# ATLAS Ω — SHORTLIST AUDIT

For each shortlist candidate record the explicit promotion reason and whether it originated from base universe or authorized external discovery.

Audit distribution by sector/capitalization/geography only to detect discovery bias; do not use those distributions as target quotas.

A suspiciously megacap/familiar shortlist should trigger discovery-process review, not automatic score penalties.
