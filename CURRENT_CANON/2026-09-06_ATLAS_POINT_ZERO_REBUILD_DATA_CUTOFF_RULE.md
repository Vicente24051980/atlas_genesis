# ATLAS Ω — DATA CUTOFF SEMANTICS

`DATA_CUTOFF` is the latest timestamp/date to which material evidence was intentionally incorporated for the canonical run.

Evidence published after the cutoff belongs to a later rerun/update and must not be backfilled into the recorded result without changing its version/provenance.

This allows fair comparison between successive portfolio states and avoids hindsight contamination.
