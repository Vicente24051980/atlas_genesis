# ATLAS Ω — CLEAN RESULT / OPERATIONAL STATE AUTHORITY

After a valid full rebuild:

- `CLEAN_SELECTED_PORTFOLIO` becomes the canonical analytical selection result for that run/data cutoff.
- `CURRENT_OPERATIONAL_PORTFOLIO` remains the factual implemented state until execution changes are confirmed.

These can differ temporarily.

Do not overwrite factual execution state merely because the clean model selected something else; do not overwrite clean analytical truth merely because execution has not yet caught up.
