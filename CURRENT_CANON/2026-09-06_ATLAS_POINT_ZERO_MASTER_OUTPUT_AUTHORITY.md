# ATLAS Ω — MASTER REBUILD OUTPUT AUTHORITY

Only the completed valid Point Zero master process can produce `CLEAN_SELECTED_PORTFOLIO` and `OPTIMAL_N` for a canonical run.

All upstream engine rankings are candidate evidence. All downstream execution states are implementation facts.

This prevents authority inversion.
