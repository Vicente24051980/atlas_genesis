# ATLAS Ω — CANDIDATE-SET INDEPENDENCE

Capital-blind scoring is insufficient if the candidate set is biased.

A clean rebuild candidate set must originate from the versioned canonical universe plus explicitly authorized external challengers, not from the current portfolio plus a small challenger list.

`UNBIASED_SCORE_ON_BIASED_CANDIDATE_SET ≠ POINT_ZERO`
