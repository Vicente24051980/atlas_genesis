# ATLAS Ω — NO CURRENT-N ANCHOR

The selection worker must not receive `CURRENT_OPERATIONAL_N=27` before deriving `OPTIMAL_N` in a blind rebuild.

Even when current holdings are hidden, exposing N=27 can anchor portfolio cardinality. Therefore current N belongs to Phase-2 execution reconciliation only.

`CURRENT_N ≠ PRIOR_FOR_OPTIMAL_N`
