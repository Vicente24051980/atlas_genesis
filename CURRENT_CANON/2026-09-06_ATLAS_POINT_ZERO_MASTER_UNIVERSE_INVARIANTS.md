# ATLAS Ω — MASTER UNIVERSE INVARIANTS

`UNIVERSE_VERSION` is mandatory in every full canonical run.

Changing the universe version is a methodological event and must be explicit/versioned. A new discovery candidate may be evaluated as an authorized external challenger, but it does not silently mutate the canonical 487-entity universe.

Raw rows, unique tickers and canonical entities are distinct concepts and must not be conflated.

Current portfolio membership does not define the universe.
