# ATLAS Ω — UNIVERSE DEDUPLICATION CONTRACT

Canonical universe identity:

`650 raw appearances → 490 unique tickers → 487 canonical economic entities`.

Deduplication occurs before scoring, shortlist formation or portfolio optimization.

Rules:

- duplicate raw appearances confer zero score and zero confidence;
- multiple listings/share classes representing one economic entity must be normalized according to the canonical entity map;
- an entity receives exactly one competitive opportunity per rebuild;
- entity aliases remain in provenance but cannot multiply membership chances;
- deduplication statistics and unresolved identity exceptions must be reported in the run audit.

A run that scores all 650 raw rows independently is non-canonical.
