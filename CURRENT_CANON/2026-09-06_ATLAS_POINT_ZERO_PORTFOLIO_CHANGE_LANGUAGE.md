# ATLAS Ω — PORTFOLIO CHANGE LANGUAGE

Use:

- `REBUILD_DIFF` for differences between clean and prior/current sets;
- `DECISIVE_REPLACEMENT` only when a clean capital duel clearly identifies a selected-vs-excluded slot substitution;
- `EXECUTED_CHANGE` only after execution is confirmed.

This avoids confusing model comparison with transaction history.
