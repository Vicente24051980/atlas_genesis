# ATLAS Ω — RECONCILIATION NO-REWRITE RULE

If execution frictions justify delaying a clean replacement, report both states:

`CLEAN_SELECTION = X`
`CURRENTLY_EXECUTED = Y`
`TRANSITION_STATUS = DELAYED/STAGED`

Do not relabel Y as the clean optimal portfolio merely because X is not yet executed.

This preserves analytical truth separately from practical implementation.
