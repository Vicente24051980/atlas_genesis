# ATLAS Ω — EXECUTION HYSTERESIS OUTPUT

When clean selection and operational state differ only marginally in estimated utility, execution may report:

`CLEAN_CHANGE_RECOMMENDED = TRUE`
`IMMEDIATE_EXECUTION = HOLD_FOR_CONFIRMATION / STAGED / EXECUTE`
`HYSTERESIS_REASON = turnover/tax/noise/uncertainty`

The clean selection itself remains unchanged.
