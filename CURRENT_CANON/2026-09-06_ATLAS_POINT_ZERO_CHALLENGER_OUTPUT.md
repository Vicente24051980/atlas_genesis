# ATLAS Ω — CHALLENGER OUTPUT REQUIREMENTS

The first 10 challengers should not be a generic watchlist. For each, report why it missed the cut and what change could promote it.

Useful fields:

`RANK_AFTER_CUT`
`TICKER`
`PRIMARY_STRENGTH`
`WHY_NOT_SELECTED`
`MARGINAL_UTILITY_GAP`
`SELECTED_NAME_MOST_AT_RISK_FROM_CHALLENGER`
`PROMOTION_TRIGGER`
`CONFIDENCE`

This makes the frontier dynamic and testable.
