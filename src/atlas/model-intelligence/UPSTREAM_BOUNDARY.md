# Upstream boundary

OmniRoute is a replaceable upstream reference and optional execution backend. ATLAS Model Intelligence Ω remains usable when OmniRoute is absent. The sync script exists for audit/comparison only; normal ATLAS routing code does not import from `.vendor/omniroute`.
