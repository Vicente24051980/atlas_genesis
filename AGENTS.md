# ATLAS Ω — Engineering Agent Contract

This repository is governed by ATLAS Ω canon.

## OpenHands / coding-agent rules
- Work on a feature branch; never commit autonomous changes directly to protected canon/main.
- Run relevant tests before proposing merge.
- Preserve FACT / HYPOTHESIS / INTERPRETATION / NOISE separation.
- Never promote external observations to canonical evidence automatically.
- Falsifiers Ω remains an independent absolute veto.
- Never commit credentials, API keys, broker secrets, tokens or private environment values.
- n8n is orchestration only; investment decisions remain inside ATLAS.
- Mem0 is retrieval context only and never canonical authority.
- Firecrawl and MarkItDown produce evidence candidates, not validated facts.
- Any broker/execution change must preserve existing execution safety gates.

## Required delivery path
issue -> branch -> implementation -> tests -> pull request -> review -> merge
