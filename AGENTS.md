# ATLAS Ω Engineering Agent Contract
OpenHands and other coding agents must use: issue -> branch -> implementation -> tests -> PR -> review -> merge.

Never commit secrets. Never auto-promote external observations to canonical evidence. Preserve FACT/HYPOTHESIS/INTERPRETATION/NOISE. Mem0 is context only. Firecrawl and MarkItDown create evidence candidates only. n8n orchestrates but has no investment-decision authority. Falsifiers Ω retains independent absolute veto. Broker execution remains behind existing safety gates.
